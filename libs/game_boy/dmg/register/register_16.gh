import ^dmg/bit/bit_16:Bit16;
import ^register/register:Register;

export alias Register16 : (16)bit;

implement Register for Register16 {
	pub flip_bit (inmut self : take Self, bit : copy Bit16) => Self {
		self[bit.index_of()] = !ditto;

		return self;
	}
}
